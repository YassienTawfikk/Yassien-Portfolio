[build]
  command = "pip install -r requirements.txt && python3 scripts/build.py"
  publish = "public"

# Disable caching for everything to ensure fresh loads
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
